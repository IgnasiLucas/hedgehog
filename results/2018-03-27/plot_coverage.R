histogram1 <- read.table('../2016-11-30/coverage.txt', col.names=c("cov", "freq", "reads"), header=TRUE)
histogram2 <- read.table('coverage.txt', col.names=c("cov", "freq", "reads"), header=TRUE)
attach(histogram1)
png(filename="coverage.png")
plot(c(0,2000), c(0,14), type='n', xlab='Pooled coverage', ylab='log(Number of loci)')
lines(cov, log(freq), col="gray")
lines(histogram2$cov, log(histogram2$freq))
dev.off()
detach(histogram1)
